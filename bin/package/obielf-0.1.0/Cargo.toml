# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.85"
name = "obielf"
version = "0.1.0"
authors = ["Nnamdi Michael Okpala / OBINexus Computing"]
build = false
include = [
    "/Cargo.toml",
    "/README.md",
    "/LICENSE",
    "/src/**/*.rs",
    "/tests/**/*.rs",
    "/docs/**/*",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Validated OBIELF part assembly, target packaging, and NASM format identifiers"
homepage = "https://obinexus.org"
documentation = "https://docs.rs/obielf"
readme = "README.md"
keywords = [
    "obinexus",
    "elf",
    "nasm",
    "bytecode",
    "packaging",
]
categories = [
    "development-tools::build-utils",
    "encoding",
]
license = "MIT"
repository = "https://github.com/obinexus/obielf"

[lib]
name = "obielf"
path = "src/lib.rs"

[[bin]]
name = "obielf"
path = "src/bin/obielf.rs"

[[test]]
name = "conformance"
path = "tests/conformance.rs"

[lints.clippy]
all = "warn"
pedantic = "warn"

[lints.rust]
unsafe_code = "forbid"
